[build-system]
requires = ['setuptools>=68']
build-backend = 'setuptools.build_meta'

[project]
name = 'looping-louie-runtime'
version = '0.0.0'
description = 'Runtime for the Looping Louie platform.'
requires-python = '>=3.12'
dependencies = []

[tool.pytest.ini_options]
asyncio_mode = 'auto'
testpaths = ['tests']
