# Harness observation fixtures

These normalized observations are mirrored verbatim at the same test path in
the API, runtime, and web repositories. Runtime tests validate the producer,
API tests validate normalization, and web tests validate consumer transforms.
Update all three copies together when the versioned contract changes.
